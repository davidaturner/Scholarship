/*
    Why is Typescript better?
    1. Detect errors before runtime.
    2. Allows scaling by working with entities vs simple data types.

    Typescript cannot run in browsers.
    Must be transpiled. (tsc)

    tsconfig.json
    https://www.typescriptlang.org/docs/handbook/tsconfig-json.html
*/