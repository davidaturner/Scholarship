/* 

An (verifiable/correct) algorithm:

    Has to have a clear problem statement.
        What is the inputs? (ex. series of values)
        What is the output? (ex. the value we are looking for)

    Must contain a specific/clear set of instructions to run in 
    a particular order to solve a problem.

    Each step must not be complicated and shouldn't be able to 
    break any step into subtasks.

    Should produce a result. Can be null.

    Should complete.

*/