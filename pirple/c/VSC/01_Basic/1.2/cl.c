#include <stdio.h>
int main()
{
    // this works better with source
    // in its own subdirectory.
    // vscode might apply to all in directory.
    // must test this.
    int number = 1000;
    printf("%d\n", number);
    return 0;
}