#include <stdio.h>
int main()
{
    // two ways to run.
    // easier way: gcc cl.c
    // then run ./a.exe
    // the other way is VSC Run
    // then run ./cl.exe
    printf("I am learning 'C' with HarryH!");
    return 0;
}