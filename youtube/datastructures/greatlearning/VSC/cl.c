#include <stdio.h>
#include <stdlib.h>

int main(int argc, char const *argv[])
{
    /*
        Stacks
        Linear data structure
        Last In, First Out ( O(1) time complexity for all operations)
        Only top element is available at all times
        Four main operations:
            push(element)
            pop()
            isEmpty()
            peek()

        Typical application:
        Recursion
        Parsing
        Browser
        Editors

    */
    struct Node
    {
        int data;
        struct Node *next;
    };
}
